<div class="page-header jumbotron subhead"><h1>New pCard</h1></div>

<%= render 'form' %>

<%= button_to "Cancel", cards_path, :method => :get, :confirm => 'Are you sure?', :type => "reset", :class => "btn btn-danger" %>
<%= render "shared/footer" %>